[core]
    autocrlf = input
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection

[merge]
    conflictStyle = zdiff3

# Default branch name
[init]
    defaultBranch = main

# Help with typos
[help]
    autoCorrect = 20

# Pull settings
[pull]
    ff = only
    rebase = true

# Pull settings
[log]
    showSignature = true

# Fix conflicts only once
[rerere]
    enabled = true

# Auto prune when fetching
[fetch]
    prune = true

[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f

[user]
    name = "op://developer/qagqzim6dvtbopwnwhtyk6zjje/username"
    email = "op://developer/qagqzim6dvtbopwnwhtyk6zjje/email"
    signingkey = "op://developer/qagqzim6dvtbopwnwhtyk6zjje/public key"

[http]
    sslVerify = true

[author]
    gpgsign = true

[commit]
    gpgsign = true

[tag]
    gpgsign = true

[gpg]
    format = ssh

[gpg "ssh"]
    program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign

; === === === Cisco Repos === === ===
[includeIf "hasconfig:remote.*.url:git@sscm.gus.cisco.com:*/**"]
    path = "${XDG_CONFIG_HOME}/git/configs/cisco"

; === === === DHL Repos === === ===
[includeIf "hasconfig:remote.*.url:https://github.com/darkhuntlabs/**"]
    path = "${XDG_CONFIG_HOME}/git/configs/darkhuntlabs"

; === === === CFS Repos === === ===
[includeIf "hasconfig:remote.*.url:https://git.va.th.ten/**"]
    path = "${XDG_CONFIG_HOME}/git/configs/cfs"

; === === === Personal Repos === === ===
[includeIf "hasconfig:remote.*.url:git@github.com:aRustyDev/**"]
    path = "${XDG_CONFIG_HOME}/git/configs/github"
