# Test npmrc file for syntax highlighting

# Basic keys
registry=https://registry.npmjs.org
loglevel=warn

# Hyphenated keys (should be highlighted)
auto-install-peers=true
strict-peer-dependencies=false
package-import-method=clone-or-copy
save-exact=true

# Namespace identifiers (should be highlighted)
@mycompany:registry=https://npm.company.com
@babel:registry=https://registry.npmjs.org
@scope:always-auth=true

# URL path keys (future feature)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
//npm.company.com/:username=myuser

# Various value types
prefix=/usr/local
cache=~/.npm
fetch-retries=3
prefer-offline=true

# Comments
; Semicolon comment (future feature)
# Hash comment