# Phase 6: Float/Decimal Support Test File
# User's Zed Theme Color Map:
# - Orange: booleans, integers, floats
# - Green: strings  
# - White: raw_values
# - Grey: comments
# - Cyan: operators
# - Red: errors/keys (variable names)

# ====================
# BASIC DECIMALS
# ====================

# Standard decimal notation
PI=3.14                    # Should be orange (float)
HALF=0.5                   # Should be orange (float)
NEGATIVE=-2.718            # Should be orange (float)
LEADING_ZERO=0.001         # Should be orange (float)

# Edge cases
ZERO_DECIMAL=0.0           # Should be orange (float)
NO_FRACTION=42.            # Should be orange (float)
NO_WHOLE=.5                # Should be orange (float)

# ====================
# SCIENTIFIC NOTATION
# ====================

# Lowercase 'e'
SMALL=1.23e-4              # Should be orange (float)
AVOGADRO_LOWER=6.022e23    # Should be orange (float)

# Uppercase 'E'
LARGE=6.022E23             # Should be orange (float)
PLANCK=6.626E-34           # Should be orange (float)

# With explicit signs
NEGATIVE_EXP=1.5e-10       # Should be orange (float)
POSITIVE_EXP=2.5e+3        # Should be orange (float)

# Integer base scientific
INT_SCIENTIFIC=1e10        # Should be orange (float)
NEG_INT_SCI=-5e6           # Should be orange (float)

# ====================
# WITH SPACING
# ====================

SPACED = 3.14              # Should be orange (float)
SPACED_SCI = 1.23e-4       # Should be orange (float)

# ====================
# QUOTED FLOATS (STRINGS)
# ====================

QUOTED_FLOAT="3.14"        # Should be green (string)
SINGLE_QUOTED='2.718'      # Should be green (string)
QUOTED_SCI="6.022e23"      # Should be green (string)

# ====================
# MIXED WITH OTHER TYPES
# ====================

# Integers still work
INT=42                     # Should be orange (integer)
NEGATIVE_INT=-100          # Should be orange (integer)

# Booleans still work
ENABLED=true               # Should be orange (boolean)
DISABLED=false             # Should be orange (boolean)

# URLs still work
API_URL=https://api.example.com/v1  # Should be white (url)

# Raw values still work
MIXED_TEXT=abc123          # Should be white (raw_value)

# ====================
# PREVIOUSLY BROKEN CASES
# ====================

# These should now parse as floats correctly
DECIMAL_PRICE=99.99        # Should be orange (float)
VERSION=3.14159            # Should be orange (float)
RATE=0.05                  # Should be orange (float)

# ====================
# STILL BROKEN CASES
# ====================

# Mixed alphanumeric with decimals
VERSION_STRING=v3.14       # "v" white, "3.14" might be orange or error
PRICE_WITH_SYMBOL=$99.99   # "$" white, "99.99" might be orange or error

# ====================
# EDGE CASES
# ====================

# Multiple dots (not a float)
IP_ADDRESS=192.168.1.1     # Should be white (raw_value)

# Hex notation (not a float)
HEX_VALUE=0xFF             # Should be white (raw_value)

# Octal notation (integer)
OCTAL=0755                 # Should be orange (integer)

# Not scientific notation
IDENTIFIER=e10             # Should be white (raw_value)
ANOTHER=E23                # Should be white (raw_value)

# Invalid floats
INVALID_FLOAT=3.           # Should be orange (float - we support this)
ANOTHER_INVALID=.          # Should be white (raw_value)
DOUBLE_DOT=3..14           # Should be complex error pattern

# ====================
# PERFORMANCE TEST
# ====================

# Many floats
F1=1.1
F2=2.2
F3=3.3
F4=4.4
F5=5.5
F6=6.6
F7=7.7
F8=8.8
F9=9.9
F10=10.10