# Phase 4 Verification File
# Open this file in Zed with the env extension loaded to verify highlighting

# ====================
# URL HIGHLIGHTING TEST
# ====================
# Expected colors in your theme:
# - URLs: Should appear distinct from raw_value (possibly underlined or blue)
# - Keys: Red
# - Comments: Grey

# Simple URLs
WEBSITE=https://example.com
API_BASE=http://api.example.com/v1

# URLs with authentication
PRIVATE_REPO=https://user:token@github.com/company/repo.git
DB_CONNECTION=https://admin:password@db.example.com:5432

# URLs with query and fragment
SEARCH=https://search.example.com/results?q=hello+world&page=2#top
DOCS=https://docs.example.com/guide/installation#prerequisites

# ====================
# URI HIGHLIGHTING TEST
# ====================
# Various URI schemes should all highlight as URLs

# Database URIs
POSTGRES=postgresql://user:pass@localhost:5432/mydb
MYSQL=mysql://root:secret@127.0.0.1:3306/database
MONGO=mongodb://localhost:27017/myapp
REDIS=redis://localhost:6379/0

# File and VCS URIs
CONFIG=file:///home/user/config.yml
REPO=git+ssh://git@github.com/user/repo.git
FTP=ftp://files.example.com/public/data.zip

# Messaging URIs
WEBSOCKET=ws://localhost:8080/socket
SECURE_WS=wss://secure.example.com:443/realtime
AMQP=amqp://guest:guest@rabbitmq:5672/vhost

# Cloud storage
S3_BUCKET=s3://my-bucket/path/to/files
GCS_BUCKET=gs://my-bucket/data/

# Other URIs
SUPPORT=mailto:support@example.com?subject=Help
DATA_URL=data:image/png;base64,iVBORw0KGgoAAAANS
LDAP=ldap://ldap.example.com:389/dc=example,dc=com

# ====================
# NOT URLs (raw_value)
# ====================
# These should appear as white (raw_value)

DOMAIN=example.com
WWW=www.example.com
MALFORMED1=http:example.com
MALFORMED2=https//example.com
PATH=/usr/local/bin:/usr/bin:/bin
DESCRIPTION=Visit us at example.com

# ====================
# MIXED TYPES
# ====================
# Testing precedence and type detection

# Quoted URLs (should be green strings, not URLs)
QUOTED_URL="https://example.com"
SINGLE_QUOTED='postgresql://localhost/db'

# Booleans and integers (orange in your theme)
ENABLED=true
PORT=8080

# Raw values (white)
SOME_TEXT=hello world
VERSION=v1.2.3

# ====================
# ERROR CASES (TODO)
# ====================
# These demonstrate where error handling would help
# Currently they parse but error nodes would make them clearer

# Multiple values on one line
BAD_MULTI=https://example.com extra text here
BOOL_EXTRA=true some more text
INT_EXTRA=12345 and then some

# Trailing content after URL
URL_TRAIL=https://api.example.com/v1, but wait there's more

# ====================
# VISUAL CHECK
# ====================
# After loading in Zed, verify:
# 1. All URLs/URIs are highlighted distinctly (not white like raw_value)
# 2. Keys are red
# 3. Strings are green
# 4. Booleans/integers are orange
# 5. Raw values are white
# 6. Comments are grey