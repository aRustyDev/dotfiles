api:
  dashboard: true
  insecure: false # Dashboard will use HTTPS

providers:
  # Docker provider - reads labels from containers
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false # Only expose containers with traefik.enable=true
    network: "traefik-public" # Default network to use
    watch: true # Watch for container changes

  # File provider - reads from config files
  file:
    directory: "/etc/traefik/dynamic"
    watch: true # Watch for file changes

ping:
  entryPoint: web

entryPoints:
  web:
    address: :80
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true

  websecure:
    address: :443
    http:
      tls: {}

# For production with Let's Encrypt (uncomment when ready)
# certificatesResolvers:
#   letsencrypt:
#     acme:
#       email: your-email@example.com
#       storage: /etc/traefik/acme/acme.json
#       httpChallenge:
#         entryPoint: web

# Logging (optional but helpful for debugging TLS)
log:
  level: INFO

accessLog:
  filePath: /var/log/traefik/access.log
