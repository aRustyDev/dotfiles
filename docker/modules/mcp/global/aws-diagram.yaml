services:
  aws-diagrams:
    image: mcp/aws-diagram
    profiles: ["project-iac", "mcp-project", "mcp"]
    networks:
      - backend        # MCP backend services network
    labels:
      traefik.enable: true
      traefik.docker.network: backend
      traefik.http.routers.aws-diagrams.rule: PathPrefix(`/aws/diagrams`)

