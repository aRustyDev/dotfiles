services:
  aws-diagrams:
    image: mcp/aws-diagram
    profiles: ["project-iac", "mcp-project", "mcp"]
    restart: unless-stopped
    networks:
      - backend        # MCP backend services network
    # Resource Tier 2: Light (diagram generation)
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M
    labels:
      traefik.enable: true
      traefik.docker.network: backend
      # Observability labels
      o11y.service: "mcp-aws-diagram"
      o11y.component: "mcp"
      traefik.http.routers.aws-diagrams.rule: PathPrefix(`/aws/diagrams`)
    logging:
      driver: "${O11Y_LOGGING_DRIVER:-json-file}"
      options:
        max-size: "${O11Y_LOG_MAX_SIZE:-10m}"
        max-file: "${O11Y_LOG_MAX_FILES:-3}"
        tag: '{{ "{{.Name}}" }}'

