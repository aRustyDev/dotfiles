services:
  aws-docs:
    image: mcp/aws-documentation
    profiles: ["aws", "docs"]
    networks:
      - backend        # MCP backend services network
    labels:
      traefik.enable: true
      traefik.docker.network: backend
      traefik.http.routers.aws-docs.rule: PathPrefix(`/aws/docs`)

networks:
  backend:
    external: true
